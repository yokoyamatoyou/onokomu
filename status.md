```markdown
# 進捗状況

**全体ステータス:** 進行中

**変更ログ:**
- 2025-08-11: `requirements.txt` に `rank-bm25` を追加しました。（ルール対象ファイルの変更）
- 2025-08-11: `src/core/document_manager.py` の `import` 文をクリーンアップしました。

---

### タスクリスト

- [x] **中核アルゴリズムの再構築 (`src/core/ocr_processor.py`)**
- [x] **必須メタデータの追加**
- [x] **既存の構文エラー修正**

- [ ] **ハイブリッド検索の実装**
    - [x] `requirements.txt` に `rank-bm25` を追加する
    - [ ] ドキュメント処理パイプラインにBM25インデックスの作成・保存処理を追加する (進行中)
    - [ ] `RAGEngine` にBM25とベクトル検索を組み合わせた検索ロジックを実装する (進行中)

- [ ] **全チャンクへのリッチメタデータ付与**
    - [ ] `DocumentManager` を修正し、チャンクごとにGPT-5miniでメタデータを生成する処理を追加する (進行中)

- [ ] **(将来課題) 混合コンテンツファイルの対応**
    - [ ] PDFやDOCXファイル内の画像を認識し、画像処理エンジンに連携する機構を設計・実装する
```
